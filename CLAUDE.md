- Never create git worktrees
- Don't commit unless I explicitly ask
- Don't push under any circumstances

- Never add code comments unless I ask
- Don't add console.logs for debugging
- Prefer early returns over nested conditionals
- Don't refactor code outside the scope of the current task

- Only modify files directly related to the task
- Don't "clean up" or improve things you notice along the way
- Ask before creating new files

- Flag blockers immediately rather than working around them

- Don't write tests unless asked
- Run existing tests after making changes and report failures

- Don't install new packages without asking first
- Use existing component patterns in the codebase before creating new ones

## Session wrap-up
When asked to summarize the session, write the summary to SESSION_NOTES.md 
in the project root with a timestamp. Append, don't overwrite.
